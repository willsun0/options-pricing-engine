# Core numerics — needed for every phase.
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7

# Testing.
pytest>=7.4

# Phase 4: live market data. Only needed to *fetch* a fresh option chain. The
# committed fixture and the cached snapshot let the tests and the Phase 4
# notebook run offline without it.
yfinance>=0.2.40

# Phase 5: the neural network volatility surface. Only imported by
# options_engine.vol_surface.ml_surface and the Phase 5 notebook — the rest of the
# engine imports fine without it, and the Phase 5 tests skip rather than fail when
# it is absent.
torch>=2.2
